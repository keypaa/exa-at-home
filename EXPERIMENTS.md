# Experiments (M1–M6 before/after)

Every optimization lands as a measured before/after row (spec §9).

| Milestone | Change | p50 before → after | recall@10 before → after | Notes |
|-----------|--------|--------------------|--------------------------|-------|
| M1 baseline | float brute force, serial | numpy exact 200×256 synth: ~1.42ms/query (wall, single query, laptop CPU) | 1.0 by construction (correctness ref) | `ann_core.AnnIndex` exact float dot; `cargo bench` latency to follow (Task 12). Rust unit + pytest green on networked toolchain; sandbox run limited to numpy-exact reference (see task-4-report). |
| M2 binary quant | sign bit per dim (256f → 32B, 32x), naive per-bit `binary_dot_packed` | Rust latency pending on-box bench (Task 12); numpy exact 500×256 here: ~0.043ms/query for scale context | 0.4460 (numpy reference of `binary_dot_packed` vs `ground_truth.py` exact, 500-doc synth, 100 queries) | Expected dip vs M1 that M5 recovers. Bit-order contract: LSB-first (`packbits bitorder="little"`); bare-packbits default verified WRONG vs `binarize`. Rust `from_binary`/`search_binary` + `cargo test`/pytest need on-box `maturin develop` (sandbox: PyO3 0.22 vs Python 3.14 ABI gate, no egress). |
| M3 IVF routing | coarse quantizer K=16 + inverted lists, nprobe sweep | numpy reference 2000×256 synth: 2.573ms/query (nprobe=16, full scan) → 0.181ms (nprobe=1); Rust latency pending on-box bench (Task 12) | 0.2080 (nprobe=1) → 0.2960 (2) → 0.4200 (4) → 0.6580 (8) → 1.0000 (16=K, exact vs M2 brute force by construction) | Recall climbs ~linearly in nprobe, latency ~linear; knee at nprobe=4–8 is the operating point. Synth uses random-subset centroids (no sklearn in Tier 0); cloud training uses `scripts/train_centroids.py` (MiniBatchKMeans). `ann_core.IvfIndex` + `cargo test`/pytest need on-box `maturin develop` (sandbox: PyO3 0.22 vs Python 3.14 ABI gate, no egress). |
| M4 LUT scoring | per-query 64×16 LUT (`lut::build` + 64 nibble-gather `lut::score`) replaces naive 256-iter `binary_dot_packed` loop in IVF scan; both fns kept, benched head-to-head | `cargo bench` `search_bench` (2000 docs, in-Rust xorshift): `binary_dot_packed_scan_2k` 1.978ms → `lut_scan_2k` 60.15µs (~33x, incl. per-query LUT build) | 1.0000 vs M2 brute force (top-10 order identical; LUT==naive score diff ≤ ~2e-6 fp reassociation, bar 1e-3) | No recall change possible beyond fp noise: same dot, reordered adds. `cargo test`/`cargo bench` ran in sandbox via vendored registry + `PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1`; pytest needs on-box `maturin develop`. |
| M5 rerank | cross-encoder `ms-marco-MiniLM-L6-v2` rerank of 200 coarse candidates → top-10, batch 128, pair truncation 160 tokens, fp16 + warmup, timeout 100ms → degraded fallback | pending cloud GPU run (design: 200 pairs ~6–10ms at batch 128 fp16, inside the 40ms rerank slice; `test_real_model_200_pairs_under_40ms` asserts it) | pending cloud GPU run (design: recovers the M2 0.4460 dip — rerank reorders coarse candidates by true query-passage relevance) | Tier-0 contract green offline: `MockReranker` reversal + injectable-timeout degraded fallback + desc-sort. Real-model + latency numbers need the GPU box (first run downloads ~90MB from HF; no egress in sandbox). |
| M6-partial filter-in-scan | roaring filter index (`scripts/build_filter.py` → `index/filter/`, Rust `filter::load` + `resolve`) intersected during the IVF scan via enriched `search(q, nprobe, top_k, allow, domains, month_range, terms)` (resolve inside Rust; bitmaps never cross FFI) | 20k-doc synth, K=64, nprobe=8, p50 over 20 queries: unfiltered 4.573ms → domain (2% sel.) 1.241ms (−3.3ms), month-quarter 3.264ms (−1.3ms), stopword-term 7.120ms (+2.5ms: single huge allow-list vs full binary-search scan) | n/a (filtering is exact row restriction: filtered ids ⊆ unfiltered ids by construction; Rust `check_row_space` + manifest pin reject stale bitmaps) | Sandbox-measured via project `.venv` `maturin develop` (real pyroaring writer → Rust `roaring` reader cross-format verified). Narrowing filters speed the scan (fewer LUT scores); a filter matching ~all rows is slower than no filter (the allow binary-search costs, scores nothing saved) — the Exa graph-vs-IVF point: intersect during scan, not post-filter. Orchestration-parallel half of M6 lands in Task 10. |
